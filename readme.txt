pwd
mkdir Websites
cd Websites/
ls -la
git clone https://github.com/MaximilianMuch/example-repo.git example_repo --> example_repo wurde ale neue Directory erstellt
cd example_repo/ --> directory wurde auf diesen Ordner gesetzt
git add readme.txt --> Staging (Vorberieten der Datein, die Übertragen werden sollen)
git status --> Abrufen des Status
git commit -m "Initial commit" --> Tell git where to go 
git push origin master --> Absenden des Codes
